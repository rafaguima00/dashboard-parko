import {
    ButtonGroup,
    Button
} from "../style";

const Buttons = () => {
    return (
        <ButtonGroup>
            <Button>Fechar caixa</Button>
            <Button>Aporte</Button>
            <Button>Retirada</Button>
        </ButtonGroup>
    )
}

export default Buttons;